import { ContadorCaracteresPipe } from './contador-caracteres-pipe';

describe('ContadorCaracteresPipe', () => {
  it('create an instance', () => {
    const pipe = new ContadorCaracteresPipe();
    expect(pipe).toBeTruthy();
  });
});
