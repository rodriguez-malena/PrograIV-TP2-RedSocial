import { ImagenBrillante } from './imagen-brillante';

describe('ImagenBrillante', () => {
  it('should create an instance', () => {
    const directive = new ImagenBrillante();
    expect(directive).toBeTruthy();
  });
});
