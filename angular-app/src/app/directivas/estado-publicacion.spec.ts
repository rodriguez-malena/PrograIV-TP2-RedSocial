import { EstadoPublicacion } from './estado-publicacion';

describe('EstadoPublicacion', () => {
  it('should create an instance', () => {
    const directive = new EstadoPublicacion();
    expect(directive).toBeTruthy();
  });
});
