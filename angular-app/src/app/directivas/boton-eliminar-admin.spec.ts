import { BotonEliminarAdmin } from './boton-eliminar-admin';

describe('BotonEliminarAdmin', () => {
  it('should create an instance', () => {
    const directive = new BotonEliminarAdmin();
    expect(directive).toBeTruthy();
  });
});
